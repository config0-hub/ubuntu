def default():

    return {
        'method': 'shelloutconfig',
        'metadata': {
            'env_vars': [],
            'shelloutconfigs': ['config0-publish:::ubuntu::install-docker-pkgs']
            }
    }
